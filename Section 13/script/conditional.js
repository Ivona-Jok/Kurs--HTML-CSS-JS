
const myName = 'Ivona';
    if (myName === 'Ivona') {
        console.log('Hello!'); 
    }

let isLoggedIn = true;
    if (isLoggedIn) {
        console.log('User is logged in!');
    }

const enteredUserName = 'Joketic';
    if (enteredUserName) {
        console.log('Input is valid');
    }